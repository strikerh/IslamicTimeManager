<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hazemnabil.islamictodo2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".addTask.AddTask2"
            android:label="@string/title_activity_add_task2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <!-- <activity -->
        <!-- android:name=".CalenderMonth" -->
        <!-- android:label="@string/title_activity_calender_month" -->
        <!-- android:theme="@style/AppTheme"></activity> -->
        <!-- <activity -->
        <!-- android:name=".calenderMonth_old.CalenderMonth2" -->
        <!-- android:label="@string/title_activity_calender_month2" -->
        <!-- android:theme="@style/AppTheme" /> -->
        <activity
            android:name=".calenderDay.CalenderDay"
            android:label="@string/title_activity_calender_day"
            android:theme="@style/AppTheme"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            />
        <!--android:configChanges="orientation|screenSize|keyboardHidden"-->
        <!-- <activity -->
        <!-- android:name=".Testtt2Activity" -->
        <!-- android:label="@string/title_activity_testtt2" -->
        <!-- android:theme="@style/AppTheme" /> -->
        <!-- <activity -->
        <!-- android:name=".calenderMonth2_old.CalenderMonth3Activity" -->
        <!-- android:label="@string/title_activity_calender_month3" -->
        <!-- android:theme="@style/AppTheme" /> -->
        <activity
            android:name=".monthCalender.ActivityMonth"
            android:label="@string/title_activity_calender_month3"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".StartActivity"
            android:theme="@style/SplashTheme">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".removed.addTask3.AddTask3Activity"
            android:label="@string/title_activity_add_task3"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".viewTask.ViewTask"
            android:label="@string/title_activity_view_task"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".viewTask.ScrollingTaskView"
            android:label="@string/title_activity_scrolling_task_view"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".viewTask.TaskItemListActivity"
            android:label="@string/title_taskitem_list"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".viewTask.TaskItemDetailActivity"
            android:label="@string/title_taskitem_detail"
            android:parentActivityName=".viewTask.TaskItemListActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hazemnabil.islamictodo2.viewTask.TaskItemListActivity" />
        </activity>
    </application>

</manifest>